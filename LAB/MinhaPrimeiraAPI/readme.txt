Para criar um projeto de webAPI direto do cmd usaremos:dotnet new webapi -n MinhaPrimeiraAPI

Para abrirmos essa pasta diretamente no code basta entramos na pasta e utilizarmos o comando:code.

para rodar a app utilizamos:dotnet run

para atualizarmos a app enquanto programamos usamos o comando:dotnet watch run